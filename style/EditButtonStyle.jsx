import { StyleSheet } from "react-native";

const EditButtonStyle = StyleSheet.create({
  editButton: {
    position: 'absolute',
    bottom: 20,
    right: 20,
    // Removed backgroundColor, width, height, borderRadius
    justifyContent: 'center',
    alignItems: 'center',
  },
});


export default EditButtonStyle;